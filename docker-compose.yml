services:
  telextract:
    build: .
    container_name: telextract
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
    restart: unless-stopped
